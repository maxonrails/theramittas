<h1>Search</h1>

    <%= form_tag groups_path, :method => 'get' do %>
        <%= text_field_tag :search, params[:search] %>
        <%= submit_tag "Search" %>
    <% end %>

    <div>
        <% @groups.each do |group| %>
            <%= render 'show' %>
        <% end %>
    </div>
